# 1. 현재 폴더에 있는 모든 변경사항(파일, 폴더)을 준비 영역에 올립니다.
git add .

# 2. 제대로 올라갔는지 확인 (파일명이 초록색으로 뜨면 성공!)
git status

# 3. 버전 기록을 남깁니다.
git commit -m "Solve: 백준 2563 색종이 (구현)"

# 4. 깃허브 온라인 저장소로 보냅니다.
git push origin main