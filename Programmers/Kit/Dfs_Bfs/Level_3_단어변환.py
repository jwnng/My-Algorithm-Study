def solution(begin, target, words):
    answer = 0
    sub = 9999
    if not target in words:
        return 0
    
    for w in words:
        if begin != w:
        
    return answer

print(solution("hit","cog",["hot", "dot", "dog", "lot", "log", "cog"]))